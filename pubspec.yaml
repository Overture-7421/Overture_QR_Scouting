name: overture_qr_scouting
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0
  file_picker: ^6.1.1
  path_provider: ^2.1.3
  http: ^1.1.0
  video_player: ^2.8.2
  chewie: ^1.7.5
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/config.json
